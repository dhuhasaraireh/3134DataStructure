public class LinkedListQueue<E> {
    public static Node(E d, Node p, Node n) {
        data = d; prev = p; next = n;
    }
    Node prev;
}
    Node next;
    E data;
    Node<E> head;
    Node<E> tail;
    public enqueue(E x) { // write this};


    }
